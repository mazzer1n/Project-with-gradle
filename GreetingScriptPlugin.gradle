class GreetingScriptPlugin implements Plugin<Project> {
    void apply(Project project) {
        project.task('hi') {
            doLast {
                println 'Hi from scriptplugin'
            }
        }
    }
}

apply plugin: GreetingScriptPlugin